# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Next.js
.next/
out/
build/
dist/

# Production
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# Debug
*.tsbuildinfo
.next-env.d.ts

# Local env files
.env
.env.local
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo

# Brønnøysund bulk data (large, regenerated on demand)
data/brreg/

# Scraped / imported data artifacts (regenerated by scripts in scripts/)
data/*.jsonl
data/*.json
data/*.tsv
data/*.csv
data/*.png
data/scraped/
data/*.ndjson
data/geonorge/
data/wikipedia/

# Keep reference data used by build scripts
!data/reference/

# Raw source downloads for import scripts (large, regenerated on demand)
filer/

# Scratch/personal files, not part of the codebase
tilbakemeldinger/
ressurser/*.xlsx

# One-off dedup/debug artifacts (root-level only — NOT public/images/, which is served statically)
/images/

# Windows reserved device name (created by accident)
nul

# Temp/debug scripts
temp*.py
temp*.js
temp*.ts

.vercel
